# Diagnostics feature environment defaults
# Directory where diagnostics JSONL snapshots are stored. If empty, Electron main process resolves to app data path.
DIAGNOSTICS_STORAGE_DIR=

# Number of days to retain diagnostics snapshots before pruning.
DIAGNOSTICS_RETENTION_DAYS=30

# Disk usage threshold (in MB) to trigger a retention warning dialog.
DIAGNOSTICS_WARNING_THRESHOLD_MB=500
