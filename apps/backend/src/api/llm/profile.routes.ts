import type { FastifyInstance } from "fastify";

/**
 * Placeholder registration for LLM profile routes. Real implementations arrive in later phases.
 */
export async function registerProfileRoutes(_app: FastifyInstance): Promise<void> {
  // Routes will be implemented in subsequent tasks.
}
